<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Key2Joy.App.UserControls.Actions.Graphics.SetCursorActionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Key2Joy.App.UserControls.Actions.Graphics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="8">
        <Grid ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Cursor:"
                       VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1"
                      HorizontalAlignment="Stretch"
                      SelectedItem="{x:Bind SelectedCursor, Mode=TwoWay}"
                      ItemsSource="{x:Bind AvailableCursors}" />
        </Grid>

        <StackPanel Spacing="4"
                    Visibility="{x:Bind IsFileMode, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Cursor File:"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Column="1"
                         HorizontalAlignment="Stretch"
                         IsReadOnly="True"
                         Text="{x:Bind CursorFilePath, Mode=OneWay}"
                         VerticalAlignment="Center" />
                <Button Grid.Column="2"
                        Content="Browse..."
                        Click="BrowseButton_Click" />
            </Grid>
        </StackPanel>
    </StackPanel>
</UserControl>
