<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Key2Joy.App.UserControls.Actions.ActionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Key2Joy.App.UserControls.Actions"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="5"
          Grid.Column="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Actions that start at the trigger:"
                       VerticalAlignment="Center" />

            <ComboBox x:Name="ActionComboBox"
                      Grid.Column="1"
                      ItemsSource="{x:Bind ActionsAvailable, Mode=OneWay}"
                      SelectedItem="{x:Bind ActionSelected, Mode=TwoWay}"
                      Margin="10,0,0,0"
                      HorizontalAlignment="Stretch">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="local:ActionComboBoxItem">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <FontIcon FontSize="16"
                                      Glyph="{x:Bind TextGlyph}"/>

                            <TextBlock Text="{x:Bind Description}"
                                       Grid.Column="1"/>
                        </Grid>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <ScrollViewer Grid.Row="1">
            <Frame Margin="0,10,0,0"
               VerticalContentAlignment="Top"
               Content="{x:Bind ActionContent, Mode=OneWay}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
