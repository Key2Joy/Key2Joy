<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Key2Joy.App.UserControls.Triggers.Mouse.MouseMoveTriggerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Key2Joy.App.UserControls.Triggers.Mouse"
    xmlns:mapping="using:Key2Joy.Mapping"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnSpacing="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Mouse Direction:"
                   VerticalAlignment="Center"/>

        <ComboBox Grid.Column="1"
                  HorizontalAlignment="Stretch"
                  SelectedItem="{x:Bind SelectedAxisDirection, Mode=TwoWay}"
                  ItemsSource="{x:Bind AvailableAxisDirections}">
            <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="mapping:AxisDirection">
                    <TextBlock Text="{x:Bind Converter={StaticResource EnumToStringConverter}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
