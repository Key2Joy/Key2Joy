# `Window.Minimize` (```String```, ```String```)

Minimize a window by a class name / title.

## Parameters
* **windowTitle (```String```)** 
	Window title to find and minimize

* **className (```String```)** 
	Optional window class name


