# `Window.Focus` (```String```, ```String```)

Focus (bring to front) a window by a class name / title.

## Parameters
* **windowTitle (```String```)** 
	Window title to find and focus

* **className (```String```)** 
	Optional window class name


